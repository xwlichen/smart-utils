apply plugin: 'com.novoda.bintray-release'

android {
    lintOptions {
        abortOnError false
    }
}

publish {
    userOrg = 'smartlibs'      // maven 的组织名
    groupId = 'com.samrt.componet' //包名
    artifactId = 'smart-utils'// bintray创建的package
    desc = 'android utils'
    publishVersion = '1.0.1'
}